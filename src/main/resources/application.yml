server:
  port: 8081

#DB ?? ??
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: wera4677
    driver-class-name: com.mysql.cj.jdbc.Driver

#JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate: { dialect: org.hibernate.dialect.MySQLDialect, format_sql: true }

